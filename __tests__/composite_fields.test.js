describe('DTO', () => {
  describe('exploding fields', () => {
    test.todo('reads and writes exploding field')
    test.todo('works with optional when all dependencies are undefined')
    test.todo('works with nullable when all dependencies are null')
  })

  describe('imploding fields', () => {
    test.todo('reads and writes imploding field')
    test.todo('works with optional when all dependencies are undefined')
    test.todo('works with nullable when all dependencies are null')
  })
})
