describe('DTO', () => {
  describe('custom type', () => {
    test.todo('can be read and written')
    test.todo('cannot be used with another type')
  })

  describe('custom enum', () => {
    test.todo('is available in DTO description')
    test.todo('restricts values from outside of allowed scope')
    test.todo('can have limiting sugars')
  })
})
